# End-to-End Product & A/B Analytics: Subscription Mobile App

Комплексный проект по продуктовой аналитике подписочного мобильного приложения (фитнес/медитации). Проект охватывает полный цикл работы аналитика: от проектирования схемы БД PostgreSQL и генерации реалистичного датасета до расчета ключевых продуктовых метрик, проведения A/B-тестирования и построения интерактивных дашбордов.

## Структура проекта

```text
.
├── analysis/
│   ├── ab_test.ipynb           # Ноутбук с дизайном, проверкой SRM и статанализом A/B-теста
│   └── metrics.ipynb           # Ноутбук с расчетом продуктовых метрик, воронки и когорт
├── dashboard_demo/
│   ├── dashboard_ab.mp4
│   └── dashboard_metrics.mp4
├── scripts/
│   ├── generation.py           # Python-скрипт генерации синтетических данных
│   └── tables_creation.sql     # SQL-скрипт создания структуры PostgreSQL
├── .env                        # Конфигурация подключения к БД
├── .gitignore
├── erd.pgerd                   # ER-диаграмма базы данных
└── requirements.txt            # Зависимости Python

```

## База данных и пайплайн данных

Данные генерируются скриптом `scripts/generation.py` и загружаются в PostgreSQL. Схема состоит из 4 реляционных таблиц (`erd.pgerd`):

1. **users**: 12 000 уникальных пользователей за 6 месяцев (сентябрь 2025 — февраль 2026), атрибуты установки (канал, устройство, страна).
2. **events**: ~50 000+ событий продуктовой воронки (`app_first_launch`, `onboarding_complete`, `paywall_view`, `subscription_purchase`, `session_start`).
3. **orders**: ~2 000+ транзакций с типами планов (weekly $4.99, monthly $14.99, annual $49.99), статусами и автопродлениями.
4. **ab_experiments**: Сплитование пользователей в эксперименте `paywall_discount_v1` (группы A и B, 50/50).

## Продуктовые метрики и юнит-экономика

В ходе анализа (`analysis/metrics.ipynb`) на основе SQL-запросов были рассчитаны ключевые показатели продукта и юнит-экономики:

* **Продуктовая воронка (Funnel & Conversions):**
  * Conversion Rate Onboarding (прохождение онбординга): **80.11%**
  * Conversion Rate Paywall (просмотр экрана подписки от прошедших онбординг): **84.70%** (сквозная конверсия от установок: ~67.85%)
  * Conversion Rate Purchase (покупка от посмотревших пейволл): **11.62%**

* **Юнит-экономика и LTV:**
  * **ARPU** (на всю аудиторию): **$1.53**
  * **ARPPU** (на платящего пользователя): **$20.69**
  * **Когортный LTV** (на примере сентябрьской когорты, size = 623):
    * LTV Day 7: **$1.05**
    * LTV Day 30: **$1.15**

* **Удержание и отток (Retention & Churn Rates):**
  * **Day 1:** Retention Rate = **17.12%** | Churn Rate = **82.88%**
  * **Day 7:** Retention Rate = **11.36%** | Churn Rate = **88.64%**
  * **Day 30:** Retention Rate = **1.34%** | Churn Rate = **98.66%**

* **Когортный анализ:** Построена понедельная матрица удержания пользователей (Retention Matrix W0–W3) с динамикой оттока по cohorts.

Также в SQL/Python построена помесячная и понедельная матрица когортного удержания (Retention Matrix).

## Анализ A/B-теста (paywall_discount_v1)

* **Гипотеза:** Внедрение скидки на первом экране подписки (Group B) увеличит конверсию в покупку без снижения удельной выручки и качества привлекаемой аудитории.
* **Проверка SRM (Sample Ratio Mismatch):** Сплитирование пользователей проверено через Z-тест долей. Различия нестатистически значимы (p > 0.05), распределение корректное (50/50).
* **Primary Metric (Conversion Rate: Paywall → Purchase):**
  * **Control (Group A):** 10.29%
  * **Treatment (Group B):** 12.59%
  * **Абсолютный прирост:** +2.30% (относительный прирост +22.35%)
  * **Статистическая значимость:** Z-score = 3.2598, p-value = 0.001115 (p < 0.05). Отклоняем нулевую гипотезу.
* **Secondary Metric (ARPU on Paywall Viewers):**
  * **Control (Group A):** $1.99
  * **Treatment (Group B):** $2.70
  * **Оценка разницы:** 95%-й доверительный интервал бутстрепа для разницы средних составил [$0.36, $1.06]. Прирост средней выручки статистически подтвержден.
* **Guardrail Metrics:**
  * **Retention D7:** Различия между группами отсутствуют (скидка не привела к оттоку пользователей после первой недели).
  * **Payment Failure Rate (status = 'failed'):** Доля технических сбоев при оплате осталась на базовом уровне.

**Итоговое бизнес-решение:** Эксперимент признан успешным. Рекомендовано выкатить вариант B с обновленным экраном подписки на 100% пользователей.

## Дашборды (демонстрация - в ./dashboard_demo)

### 1. Продуктовый дашборд и юнит-экономика

Визуализация основных KPI, динамики установок по каналам привлечения, шагов воронки конверсии и когортной матрицы удержания.

### 2. Дашборд A/B-тестирования

Мониторинг сплита групп, сравнение ключевой конверсии, доверительных интервалов и ARPU по группам A и B.


## Запуск проекта

1. **Клонирование репозитория и установка зависимостей:**
```bash
git clone <repository_url>
cd <repository_folder>
pip install -r requirements.txt

```


2. **Настройка базы данных:**
Создайте файл `.env` в корне проекта и укажите параметры подключения:
```env
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=product_analytics

```


3. **Инициализация БД и генерация данных:**
```bash
psql -U postgres -d product_analytics -f scripts/tables_creation.sql
python scripts/generation.py

```


4. **Запуск ноутбуков:**
```bash
jupyter notebook analysis/

```
